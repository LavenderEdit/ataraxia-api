version: '3.8'

services:
  ataraxia-api:
    build: .
    container_name: ataraxia-api-container
    ports:
      - '8081:8081'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
    networks:
      - portfolio_default

networks:
  portfolio_default:
    driver: bridge
